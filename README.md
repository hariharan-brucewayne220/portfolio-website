# portfolio-website

Personal portfolio site (Next.js 14, TypeScript).

## Deployment

The site deploys via Netlify (https://hariharan-l-portfolio.netlify.app), which builds from this repo on push to `main`. There is no GitHub Actions deploy step.

## Local development

```bash
npm ci
npm run dev
```
